# stigref — Detailed Analysis (July 2026)

**Date:** 2026-07  
**Scope:** Full project review (pipeline, data, frontend, CI).  
**Status:** Findings **accepted**; remediations tracked and largely **shipped**.  
**Last re-audit:** 2026-07-10 (post B-041) against live `main`.

---

## Overall verdict (original)

The project is in strong shape for a small, focused repo. Weaknesses concentrated in:

1. Error handling that can hide failures in the data pipeline  
2. Operational gaps (stale KEV, no refresh automation, unpinned deps)  
3. Frontend polish (no tests, thin a11y, UX dead-ends)

Site: https://edodev.github.io/stigref/

---

## Disposition matrix (Claude review → today)

### Suggested priorities (original §)

| # | Claude recommendation | Disposition | Evidence |
|---|----------------------|-------------|----------|
| 1 | Refresh KEV + scheduled KEV workflow | **Accepted & done** | `threat_enrich.py` retry/validate; `kev-refresh.yml`; `data/threat/meta.json` fetched 2026-07-10 |
| 2 | Pipeline error-handling pass | **Accepted & done** | Narrowed catches; stage failures at **error**; intentional catch-alls documented (`library.py` meta errors) |
| 3 | `data/releases/{date}/` before next DISA import | **Accepted — foundation done** | `meta.currentRelease` / `releases[]`; `data/releases/index.json` + pointer; `promote_current.py` / `diff_releases.py`. **Full second-quarter tree still waits for next library ZIP** |
| 4 | Pin deps, Dependabot, SHA Actions, no prod sourcemaps | **Accepted & done** | `requirements.txt` pinned; `.github/dependabot.yml`; Pages/CI/KEV SHA pins; `sourcemap: false` |
| 5 | Frontend UX/a11y round | **Accepted & done** | Skip link, live regions, `ErrorRetry`, KEV show-more, search URL debounce, `aria-pressed`, wider layout, Carbon locked |
| 6 | Testing + CI | **Accepted & done** | Vitest (28) + pytest (40+); `.github/workflows/ci.yml`. Residual: no Playwright e2e; no full real DISA ZIP in CI |
| 7 | Process: backlog IDs, B-018 runbook, size metrics | **Accepted & done** | Many commits cite B-IDs; `INTUNE_CSP.md` B-018 runbook; `meta.sizes` (~117 MB raw data) |

### Findings by area (original detail)

#### Data pipeline

| Finding | Status | Notes |
|---------|--------|-------|
| KEV silent fail / stale cache crash | **Fixed** | Retries, JSON validation, min-vuln floor, safe cache read |
| Blanket `except Exception` drops stages | **Fixed (pragmatic)** | Narrowed types + `log.error` / `log.exception`; fail-soft where enrichment is optional |
| Unpinned requirements / no pyproject | **Fixed (pin)** | Exact pins; Dependabot for pip. No `pyproject.toml` (accepted residual) |
| Fragile `__file__` paths | **Accepted residual** | Still package-relative; works in our layout |
| Low test ratio / no real ZIP / no KEV network tests | **Partially fixed** | Real suite growth + KEV validation unit tests; nested ZIP covered in `test_build_data`; **no live network KEV in CI** (by design) |

#### Data

| Finding | Status | Notes |
|---------|--------|-------|
| KEV snapshot stale (Apr) | **Fixed** | Catalog 2026.07.07; weekly workflow |
| CVE∩KEV near empty | **Accepted fact** | Structural XCCDF limit; KEV tab standalone still useful |
| Repo growth / multi-release | **Foundation done** | Registry + promote/diff; search index now **~2.6 MB gzip shards** (was ~24 MB monolith). Full dual-quarter catalogs on **next DISA import** |

#### Frontend

| Finding | Status | Notes |
|---------|--------|-------|
| Zero tests | **Fixed** | Vitest unit tests for core libs |
| Prod source maps | **Fixed** | `sourcemap: false` |
| Thin a11y | **Fixed (v1)** | Skip, live regions, labels, pressed state; not full WCAG audit |
| KEV hard-cap 200 | **Fixed** | Paginated “show more” |
| Fetch errors no retry | **Fixed** | `ErrorRetry` on main error states |
| Search every keystroke + `replaceState` | **Fixed** | Debounced URL updates (250 ms) |
| `execCommand` clipboard fallback | **Accepted residual** | Still present as last-resort fallback after Clipboard API |
| Only two breakpoints | **Improved** | +480px pass; layout max **1280px** |
| No prerender / SEO shell | **Accepted residual** | Still SPA empty without JS — deferred product choice (static reference SPA) |
| RuleDetail large file | **Accepted residual** | Partially mitigated by components; not fully split into four files |

#### CI / process

| Finding | Status | Notes |
|---------|--------|-------|
| Only Pages workflow | **Fixed** | + `ci.yml` + `kev-refresh.yml` |
| No Dependabot / tag-only Actions | **Fixed** | Dependabot + SHA pins |
| Commits lack backlog IDs | **Improved** | Many commits now cite B-xxx; not enforced by hook |
| B-018 runbook missing | **Fixed** | `docs/INTUNE_CSP.md` |
| DESIGN size budget unvalidated | **Fixed (raw sizes)** | `meta.sizes`; transfer gzip for search validated via shards |

---

## Acceptance checklist (updated)

### Original remediation wave

- [x] `docs/REVIEW_2026-07.md` committed  
- [x] KEV: retries, validation, guarded cache  
- [x] Scheduled KEV refresh workflow  
- [x] Pipeline stage failures visible as errors  
- [x] Python deps pinned  
- [x] Prod sourcemaps off  
- [x] Dependabot (npm/pip/actions)  
- [x] Actions SHA-pinned  
- [x] Live KEV snapshot refreshed  

### Deferred items from original review (now addressed)

- [x] Multi-release foundation (B-021) — full dual tree on next import  
- [x] Frontend tests + CI (B-075)  
- [x] a11y/UX round (B-074+)  
- [x] Search worker (B-040)  
- [x] Compressed/sharded index (B-041)  
- [x] ATT&CK / threat epic v0 (B-062+)  
- [x] MS Windows Intune processing expand (B-012)  

### Explicitly accepted residuals (not defects)

| Item | Why accepted |
|------|----------------|
| No full HTML prerender | SPA product; cost vs value; Pages serves app shell |
| `execCommand` clipboard fallback | Harmless last resort |
| No e2e / no live network tests in CI | Speed + flakiness; unit + build gate sufficient for now |
| Dual-quarter STIG trees not in git yet | Requires July+ DISA library import; tooling ready |
| CIS IDs partly operator-seeded | Mapping-only; verify against Workbench PDF |
| Catch-all parse errors | Intentional so one bad XCCDF does not abort library build |

---

## Original findings text (archive)

### What's working well

- Current stack, minimal surface (Svelte 5, Vite 6, MiniSearch)  
- Sound pipeline architecture  
- Exceptional docs / backlog  
- Feature claims verified in `web/src`  

*(Unchanged — still accurate.)*

---

## Related docs

- [BACKLOG.md](BACKLOG.md)  
- [VERSION_HISTORY.md](VERSION_HISTORY.md)  
- [PLAN_THREAT_INTEL.md](PLAN_THREAT_INTEL.md)  
- [CIS_LOCAL.md](CIS_LOCAL.md)  
- [SESSION_LOG.md](SESSION_LOG.md)  
